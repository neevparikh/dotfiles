#!/usr/bin/env bash

if [[ $@ = "--dark" ]] 
then
    sed -i "" "s/colors: \*gruvbox_light/colors: \*gruvbox_dark/" $HOME/.alacritty.yml
    echo 'dark' > $HOME/.config/theme.yml
elif [[ $@ = "--light" ]] 
then
    sed -i "" "s/colors: \*gruvbox_dark/colors: \*gruvbox_light/" $HOME/.alacritty.yml
    echo 'light' > $HOME/.config/theme.yml
fi 
