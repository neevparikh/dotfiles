#!/bin/bash
DIR="$( cd "$( dirname "${BASH_SOURCE[0]}" )" && pwd )"
export LD_LIBRARY_PATH=$DIR/../lib:$LD_LIBRARY_PATH
cd $DIR
$DIR/jre/bin/java -Djdk.gtk.version=2 -jar $DIR/STM32CubeProgrammerLauncher