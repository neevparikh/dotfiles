#!/usr/bin/env bash

source $HOME/.local/bin/toggle-theme

while IFS= read -r line; do
    if [[ $line = "dark" ]] 
    then
      bat --theme=Catppuccin-$DARK $@
    elif [[ $line = "light" ]] 
    then
      bat --theme=Catppuccin-$LIGHT $@
    fi 
done < $HOME/.config/theme.yaml
